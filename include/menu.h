#ifndef MENU_H
#define MENU_H

namespace Menu {
    // Muestra en la pantalla las opciones que el usuario puede elegir (el menú principal)
    void showMenu();
    
    // Hace funcionar todo el programa, manteniendo el menú activo y recibiendo lo que el usuario escribe
    void runApp();
}

#endif // MENU_H
